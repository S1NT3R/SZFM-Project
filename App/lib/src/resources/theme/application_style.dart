import 'package:flutter/material.dart';

class ApplicationStyle {
  static const Color white = Color(0xffFFFFFF);
  static const Color black = Color(0xff000000);
  static const Color backgroundColor = Color(0xFF487AC6);
  static const Color transparent = Colors.transparent;

  static const Color primaryColor = Color(0xFF487AC6);
  static const Color textColor = Color(0xFFC23CB4);

  static const Color inputErrorColor = Colors.red;
  static const Color disabledInputColor = Color(0xffE1DEDE);

  static const Color processIndicatorColor = Color(0xFFC23CB4);

  static const Color dividerColor = Color(0xffFFFFFF);

  static const double buttonHeight = 62.0;
  static const double mediumButtonHeight = 42.0;
  static const double smallButtonHeight = 28.0;

  static const double borderRadius = 50.0;
  static const double iconSize = 32.0;

  static BorderRadius borderRadiusCircular =
      BorderRadius.circular(borderRadius);
}
